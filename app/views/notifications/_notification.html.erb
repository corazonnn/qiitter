<div class="ui segment">

<div class="notification-view"><!--app/helprs/notifications_helper.rb内のメソッド-->
  <h4 class="ui header">
  <%= notification_form(notification) %>
  <div class="sub header"><span class="moderate-font"><%= " (#{time_ago_in_words(notification.created_at)}前)" %></span></div>
</h4>

  <br>
  <% if !@comment.nil? %>
    <p class="notification-comment"><%= @comment %></p>
  <% end %>
</div>
</div>
